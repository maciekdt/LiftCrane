export default class Task {
    constructor(
        id,
        liftName,
        location,
        date,
        type
    ) {
        this.id = id
        this.liftName = liftName
        this.location = location
        this.date = date
        this.type = type
    }
}